import React from 'react';
import { Container, Typography } from '@mui/material';

const Groups = () => {
    return (
        <Container>
            <Typography variant="h4">Groups</Typography>
            <Typography>List of groups will be displayed here.</Typography>
        </Container>
    );
};

export default Groups;
